{"v":1,"ts":"2026-02-15T19:25:00.000Z","session":"main","type":"discovery","status":"complete","title":"Stratus API authentication breakthrough - auth cache discovery","summary":"Debugged and resolved Stratus API integration authentication issues by discovering OpenClaw's dual auth storage (config + cache). API now authenticating successfully with 17784 credits confirmed. Identified next blocker: role field compatibility.","detail":"AUTHENTICATION BREAKTHROUGH: After extensive debugging, discovered that OpenClaw maintains TWO separate locations for API authentication: (1) Config file ~/.openclaw/openclaw.json (what users edit) and (2) Auth cache ~/.openclaw/agents/main/agent/auth-profiles.json (what the gateway actually uses). The config file does NOT automatically sync to the auth cache, causing silent failures. Solution: Updated auth cache with Stratus credentials, restarted gateway (openclaw gateway restart), verified with curl. Results: API endpoint dev.api.stratus.run responding correctly, API key validated, user balance confirmed at 17784.00 credits, model stratus-x1ac-base-claude-sonnet-4-5 available. NEW BLOCKER DISCOVERED: Role field incompatibility - OpenClaw sends 'role: developer' (Anthropic-specific) but Stratus/Anthropic expects 'role: user' or 'role: assistant' when using OpenAI-compatible /v1/chat/completions endpoint. Error: 'messages: Unexpected role developer. Allowed roles are user or assistant'. Next step: Implement role mapping in plugin (developer → system) to achieve compatibility.","files":["TROUBLESHOOTING.md","knowledge/INTEGRATION_STATUS.md"],"learned":["OpenClaw has dual auth storage: config file + auth cache (auth-profiles.json)","Config changes don't auto-sync to auth cache - must update both manually","Auth cache staleness causes silent fallback to other providers","401 errors indicate auth cache issue, not endpoint issue","Always verify auth independently with curl before debugging plugin code","The 'developer' role is Anthropic-specific and not supported in OpenAI-compatible APIs"],"incomplete":["Role mapping implementation in plugin","Chat completion test with role mapping","Full integration test"],"next":"Implement role mapping in src/client.ts: normalizeRole() function to transform developer → system before API calls"}
